export interface UpdateCheckoutShippingNip {
    /**
     * Update shipping address in the existing checkout.
     */
}
  
export interface UpdateCheckoutShippingNipVariables {
    checkoutId: string;
    nip: string;
}